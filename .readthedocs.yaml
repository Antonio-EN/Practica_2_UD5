version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

# Cambiamos sphinx por mkdocs
mkdocs:
  configuration: mkdocs.yml

# Si no tienes un archivo requirements.txt, puedes simplificarlo así:
python:
  install:
    - method: pip
      path: .
